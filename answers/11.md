# Lecture 11 Answers

## Key Concepts

**Clustering vs. classification:** classification assigns documents to predefined classes using labeled training data (supervised). Clustering groups documents by similarity with no predefined classes and no labels (unsupervised).

**Cluster hypothesis:** documents in the same cluster tend to be relevant to the same information needs. If true, retrieving one relevant document from a cluster makes all other cluster members good candidates too.

**Clustering in IR:** used for organizing search results, improving recall via cluster-based retrieval, building document collections for browsing, and speeding up nearest-neighbor search.

**Flat clustering:** produces a single partition of documents into K clusters with no hierarchy. K-means is the canonical example. Fast but requires specifying K in advance.

**Hierarchical clustering:** produces a tree (dendrogram) of nested clusters. Does not require K in advance; the desired level of granularity can be chosen after the fact by cutting the tree at a given height.

**Hard clustering:** each document belongs to exactly one cluster. K-means is a hard clustering algorithm.

**Soft clustering:** each document can belong to multiple clusters with varying degrees of membership (probabilities). Gaussian Mixture Models (EM algorithm) are the canonical example.

**K-means algorithm:** (1) initialize K centroids; (2) assign each document to its nearest centroid; (3) recompute centroids as the mean of assigned documents; (4) repeat until convergence.

**K-means convergence vs. optimality:** K-means always converges (the objective — residual sum of squares — decreases monotonically), but is not guaranteed to find the global optimum; it may converge to a local minimum depending on initialization.

**Residual Sum of Squares (RSS):** the K-means objective function — the sum of squared distances from each point to its assigned centroid. Minimizing RSS gives tighter, more compact clusters.

**Purity:** an external clustering evaluation metric. For each cluster take the majority class; purity = (1/N) × Σ (size of majority class in cluster k). Ranges from 0 to 1; higher is better.

**Rand index (RI):** an external metric counting the fraction of document pairs for which the clustering and the ground-truth class assignment agree (both in same cluster, or both in different clusters). RI = (TP + TN) / (TP + FP + FN + TN).

**Optimization of number of clusters:** choose K by plotting RSS vs. K and looking for an "elbow," or by using information-theoretic criteria (AIC, BIC) that penalize model complexity.

**Agglomerative hierarchical clustering (HAC):** bottom-up — start with each document as its own cluster and repeatedly merge the two most similar clusters until only one remains.

**Bisecting K-means:** a top-down hierarchical method that starts with one cluster and repeatedly splits the largest (or least coherent) cluster using K-means with K=2.

**Cluster similarity:** the measure used in HAC to decide which two clusters to merge. Common choices: single-link (max similarity between any pair of members), complete-link (min similarity), GAAC (average over all cross-cluster pairs), centroid (similarity of centroids).

**Chaining:** a problem with single-link clustering where loose, elongated clusters form because merging is triggered by the single closest pair of members, eventually linking unrelated documents through a chain of intermediate points.

**Inversions:** a problem in HAC where a cluster formed later in the hierarchy has a higher inter-cluster similarity than a cluster formed earlier, violating the monotonicity of the dendrogram. Can occur in centroid clustering.

## Exercise 11.1

> Define two documents as similar if they have at least two proper names like Clinton or Sarkozy in common. Give an example of an information need and two documents, for which the cluster hypothesis does not hold for this notion of similarity.

The cluster hypothesis says that similar documents tend to be relevant to the same information needs. With the proposed similarity definition, two documents are similar if they share at least two proper names.

Counterexample information need:

```text
Find documents about scandals involving Bill Clinton and Nicolas Sarkozy.
```

Document 1:

```text
Bill Clinton and Nicolas Sarkozy met at an international summit to discuss climate policy.
```

Document 2:

```text
Bill Clinton and Nicolas Sarkozy were both mentioned in a retrospective article about political scandals.
```

The documents share the proper names `Clinton` and `Sarkozy`, so they are similar by the proposed definition. But only Document 2 is relevant to the information need. The shared names alone do not capture the user's intended relation or topic.

## Exercise 11.2

> Make up a simple one-dimensional example (i.e. points on a line) with two clusters where the inexactness of cluster-based retrieval shows up. In your example, retrieving clusters close to the query should do worse than direct nearest neighbor search.

Consider one-dimensional documents on a line:

```text
Cluster A: 0, 100
Cluster B: 49, 51
Query: 1
```

If we retrieve by direct nearest neighbor search, the nearest document to the query is:

```text
0
```

which is in Cluster A.

But if cluster-based retrieval represents each cluster by its centroid:

```text
centroid(A) = (0 + 100) / 2 = 50
centroid(B) = (49 + 51) / 2 = 50
```

Cluster A and Cluster B look equally close by centroid, even though Cluster A contains the true nearest neighbor. With a small perturbation:

```text
Cluster A: 0, 102     centroid = 51
Cluster B: 48, 50     centroid = 49
Query: 1
```

Cluster B's centroid is closer to the query, but the nearest individual document is still `0` in Cluster A. This shows the inexactness of cluster-based retrieval: a cluster summary can hide a very close document.

## Exercise 11.3

> Purity as an external evaluation criterion for cluster quality. Majority class and number of members of the majority class for the three clusters are: x, 5 (cluster 1); o, 4 (cluster 2); and ⋄, 3 (cluster 3). Purity is (1/17) × (5 + 4 + 3) ≈ 0.71.
>
> Replace every point d with two identical copies of d in the same class in the above figure.
>
> 1. Is it less difficult, equally difficult or more difficult to cluster this set of 34 points as opposed to the 17 points in the figure?
> 2. Compute purity and RI for the clustering with 34 points. Which measures increase and which stay the same after doubling the number of points?
> 3. Given your assessment in (i) and the results in (ii), which measures are best suited to compare the quality of the two clusterings?

The original slide has 17 points and 3 clusters. From the slide's counts:

```text
Cluster 1: x = 5, o = 1
Cluster 2: o = 4, diamond = 2
Cluster 3: x = 2, diamond = 3
```

The original purity is:

```text
(5 + 4 + 3) / 17 = 12/17 ≈ 0.71
```

### Difficulty after doubling

Replacing every point by two identical copies in the same class does not add new structure. Conceptually, it is equally difficult to cluster: the data set contains the same distinct point locations and labels, just duplicated.

### Purity after doubling

After doubling:

```text
Cluster 1: x = 10, o = 2
Cluster 2: o = 8, diamond = 4
Cluster 3: x = 4, diamond = 6
```

The majority counts are:

```text
10, 8, 6
```

So:

```text
purity = (10 + 8 + 6) / 34
       = 24 / 34
       = 12 / 17
       ≈ 0.71
```

Purity stays the same.

### Rand index after doubling

For the doubled data, the true positives are the same-class pairs inside clusters:

```text
TP = C(10,2) + C(2,2) + C(8,2) + C(4,2) + C(4,2) + C(6,2)
   = 45 + 1 + 28 + 6 + 6 + 15
   = 95
```

Pairs in the same cluster:

```text
TP + FP = C(12,2) + C(12,2) + C(10,2)
        = 66 + 66 + 45
        = 177
```

So:

```text
FP = 177 - 95 = 82
```

Class sizes after doubling are:

```text
x = 14, o = 10, diamond = 10
```

Same-class pairs:

```text
TP + FN = C(14,2) + C(10,2) + C(10,2)
        = 91 + 45 + 45
        = 181
```

So:

```text
FN = 181 - 95 = 86
```

Total pairs:

```text
C(34,2) = 561
```

Therefore:

```text
TN = 561 - TP - FP - FN
   = 561 - 95 - 82 - 86
   = 298
```

Rand index:

```text
RI = (TP + TN) / (TP + FP + FN + TN)
   = (95 + 298) / 561
   = 393 / 561
   ≈ 0.701
```

The original slide gives:

```text
RI_original = (20 + 72) / 136 ≈ 0.676
```

So after doubling:

```text
purity stays the same
RI increases
```

Since the doubled problem is not really a different clustering problem, an evaluation measure that stays the same is better suited for comparing these two clusterings. In this specific comparison, purity is preferable to RI. More generally, though, purity has its own problem: it can be artificially increased by using more clusters.

## Exercise 11.4

> Why are documents that do not use the same term for the concept car likely to end up in the same cluster in K-means clustering?

Documents that use different terms for `car`, such as `car` and `automobile`, can still end up in the same K-means cluster because clustering uses all vector dimensions, not only the exact word `car`.

For example, documents about cars and automobiles may share many other terms:

```text
dealer, engine, road, wheel, fuel, repair, Mercedes, vehicle
```

Even if one document says `car` and another says `automobile`, their full document vectors can be close because of these shared contextual terms. K-means groups documents by distance to centroids, so shared context can pull them into the same cluster.

## Exercise 11.5

> Two of the possible termination conditions for K-means were (1) assignment does not change, (2) centroids do not change. Do these two conditions imply each other?

The two termination conditions are:

1. assignments do not change
2. centroids do not change

If assignments do not change, then centroids do not change, because each centroid is computed as the average of exactly the same assigned points.

The reverse is not logically guaranteed in all possible data sets. Different assignments can produce the same centroid, especially when there are duplicate points or symmetric configurations. For example, two identical documents could swap between two clusters without changing either centroid.

In normal K-means with deterministic tie-breaking, unchanged centroids imply that the next reassignment step will also be unchanged, because the same points are compared with the same centroids. So as practical stopping tests they are closely related, but as logical statements about arbitrary assignments, only:

```text
unchanged assignments => unchanged centroids
```

is always true.

## Exercise 11.6

> For a fixed set of N documents there are up to N² distinct similarities between clusters in single-link and complete-link clustering. How many distinct cluster similarities are there in GAAC and centroid clustering?

For single-link and complete-link clustering, cluster similarity is the maximum or minimum of original document-document similarities. Therefore there can be at most about `N^2` distinct similarity values, because every cluster similarity is one of the original pairwise document similarities.

For centroid clustering and GAAC, cluster similarity is not restricted to one original document-document similarity. It is computed from averages or centroids of clusters. Different subsets of documents can produce new similarity values.

The number of possible nonempty disjoint cluster pairs is exponential. Each document can be:

```text
in the first cluster,
in the second cluster,
or in neither cluster
```

so there are on the order of:

```text
3^N
```

possible ordered disjoint cluster pairs, and therefore exponentially many possible cluster similarities. More precisely, ignoring order and excluding empty clusters, the count is:

```text
(3^N - 2 * 2^N + 1) / 2
```

So GAAC and centroid clustering can have exponentially many distinct cluster similarities, not merely `N^2`.

## Exercise 11.7

> Consider running 2-means clustering on a collection with documents from two different languages. What result would you expect? Would you expect the same result when running an HAC algorithm?

For 2-means on documents from two different languages, I would expect the two clusters to mostly correspond to the two languages.

Reason: documents in the same language share many function words and vocabulary items, while documents in different languages often have almost disjoint vocabularies. In vector space, within-language similarities will usually be much higher than cross-language similarities.

For HAC, I would also often expect the hierarchy to separate by language near the top, especially with complete-link or GAAC. But the result depends more on the linkage method:

- Single-link can be affected by chaining through shared names, numbers, URLs, or borrowed words.
- Complete-link tends to avoid loose cross-language chains.
- GAAC is often a good choice for vector representations and should usually preserve the language split.

So the broad expectation is the same, but HAC's exact behavior depends on the cluster similarity criterion.
